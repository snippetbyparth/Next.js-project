import {ReactNode} from "react";

export default function Navbar(){
    return (
        <nav className= "bg-gray-800 p-4 m-1.5 rounded-lg flex items-center justify-between">
            <h1 className="text-white text-lg">Employee Dashboard</h1>
        </nav>
    )
}